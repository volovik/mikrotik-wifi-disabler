#disable wifi if lan port connected
:global lan ether5
:global wan wlan1
:if ([interface get $lan running]=true) do={:if ([interface get $wan running]=false) do={:log info "wifi uzhe otkluchen"} else={interface wireless disable wlan1; :log info "skript vikluchil wifi"}} else={}
